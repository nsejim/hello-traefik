# hello-traefik

This repo demonstrate a basic multi-docker-compose architecture, with services organised by folder. 
This enables to have a clean, easy to read parent docker-compose that only contains the traefik container. 

Note that every docker-compose need to be started individually
